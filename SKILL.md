# SVG Flowchart

把"用内联 SVG 画流程图"从"凭感觉摆坐标"升级为"有规则、可校验、不返工"。

## 解决的问题

手动画 SVG 流程图时, 连线最容易出现三类低级错误:
- 斜线歪斜、折线乱翘
- 箭头没对准目标框
- 方块挤在一起 / 重叠

根因都是 **没有规则地随意摆坐标**。本项目把这些问题的解法固化成规则 + 校验工具。

## 核心价值

**流程图以内联 SVG 形式直接嵌入 HTML 方案文档** —— 零依赖、零外链、矢量清晰、
可被脚本自动校验。这与"贴图片 / 引第三方库 / 外链渲染"的方案相比，单文件即可交付、离线即开即看。

## 黄金规则

1. **R1 连线全正交** — 禁止对角线（指向菱形的短斜线豁免）
2. **R2 主链中心对齐 → 纯垂直** — 微调块位置对齐，而不是加折线绕
3. **R3 单折弯优先** — 分支只允许 L 形，禁止 S 形多折
4. **R4 箭头对准** — 方框顶部中心 / 菱形上顶点
5. **R5 别单挪菱形** — 菱形是夹心饼干，挪它上方的块
6. **R6 方块不重叠** — 并排框间距 ≥20px
7. **R7 少用图例** — 已用颜色区分就别加

## 用法

```bash
# 校验你的流程图 HTML / SVG
python3 scripts/validate.py your-file.html

# 提取坐标核对对齐
python3 scripts/extract.py your-file.html
```

模板见 `templates/`。详见 [README.md](README.md)。

## 目录

```
├── README.md
├── templates/      # 3 个起步模板
└── scripts/
    ├── validate.py
    └── extract.py
```
