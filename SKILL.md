---
name: svg-flowchart
description: >
  用内联 SVG 画手绘风格流程图的规范与工具。
  当用户要求生成带流程图的自包含 HTML 方案文档，且对"连线必须正交、箭头对准、无歪斜、折弯最少"有明确要求时使用本技能。
  核心方法论：块/菱形中心对齐 + 连线全正交 + 单折弯优先 + 自动校验。
---

# SVG Flowchart Skill

## 这是什么

把"用 SVG 手工绘制流程图"这件事，从"凭感觉摆坐标"升级为"有规则、可校验、不返工"。

> 背景：本项目沉淀自一次真实踩坑经历——手写 SVG 流程图时，
> 连线反复出现"斜线歪、折线乱翘、箭头没对准框、图例菱形错位"等问题。
> 根因是**没有规则地随意摆坐标**。本技能把解决这些问题的方法论固化下来。

## 适用场景

- 生成**自包含单 HTML**（浏览器直接打开，零依赖）
- 需要**精确控制布局**的复杂流程图（访客流程、坐席流程、判断分支、回流汇聚）
- 对视觉质量有高要求（不允许斜线、不允许箭头歪）

> 若只是简单逻辑、要放进 Markdown/GitHub README，**优先用 Mermaid**——
> 语法简单、可维护、生态标准。SVG 是你在"要精确布局"时的武器，不是默认选项。

## 核心原则（一句话）

**只要块和菱形中心对齐，连线就自然垂直；连得干净，靠的是对齐而不是折线绕。**

## 黄金规则（必须遵守）

### R1. 连线必须全正交
- **禁止对角线**。每根线只能是水平（H）或垂直（V）段。
- 只有"指向菱形"的短斜线可以豁免（菱形本身是斜的），但**能垂直就垂直**。

### R2. 主链中心对齐 → 连线纯垂直（最重要）
- 一条逻辑链上的节点，**垂直中心 x 全部对齐到同一个值**。
- 对齐后，块→块、块→菱形 就是**一根纯垂直竖线，零折弯**。
- 这是避免"大量折线"的根本方法：**微调块的位置，而不是加折线去绕**。

### R3. 单折弯优先
- 分支（如从菱形两侧分流）才允许拐弯，且**每个分支最多 1 个拐点**
  （水平引出 → 垂直进框）即"L 形"，禁止"S 形"多折。

### R4. 箭头必须对准目标
- 指向方框：箭头终点 = 方框**顶部中心** x。
- 指向菱形：箭头终点 = 菱形**上顶点**。
- 指向错误 = 重画，不要容忍"差几个像素"。

### R5. 菱形是"夹心饼干"，别单独挪它
- 菱形上下都连着节点，一挪就破坏上下对齐。
- **要挪就挪菱形上方的块**，让块的垂直中心 = 菱形顶点的 x。

### R6. 方块间距均匀、不重叠
- 并排分支方框之间留 ≥20px 间隙，两框绝不重叠。
- 方框足够大，能容纳文本且不贴边。

### R7. 尽量少用图例
- 已用颜色区分角色的流程，**别加图例**。图例里的小图形最容易画歪。

## 工作流

1. **确定主链**：找出流程的"主干"节点（一条直线走到底）。
2. **定主轴 x**：主链所有节点中心统一到同一 x。
3. **画分支**：分支从菱形两侧水平引出，单拐弯进框。
4. **画回流/汇聚**：如需汇聚到下游框，各自垂直落到框顶（允许落在框顶不同位置，但别落框外）。
5. **校验**：跑 `scripts/validate.py` 自动检查斜线、未对齐、重叠、折弯冗余。
6. **迭代**：按校验+人工反馈微调块坐标（不是加折线），直到全绿。

## 目录结构

```
svg-flowchart-skill/
├── SKILL.md                 # 本文件
├── templates/
│   ├── h-v-ortho.svg        # 正交连线基础模板
│   ├── decision-branch.svg  # 判断菱形 + 三分支模板
│   └── chain-vertical.svg    # 纯垂直主链模板
├── scripts/
│   ├── validate.py          # 自动校验：斜线/对齐/重叠/折弯
│   └── extract.py           # 从 HTML 提取并解析所有 <svg>
├── examples/
│   └── flowchart.html       # 完整示例：三张流程图的方案文档
└── README.md                # 项目说明
```

## 校验指标（validate.py 检查项）

| 检查 | 规则 | 说明 |
|------|------|------|
| 斜线 | = 0 | 不允许对角线（菱形短斜线除外） |
| 箭头对准 | 误差 ≤ 2px | 方框中心 / 菱形上顶点 |
| 方块重叠 | = 0 | 并排框间距 ≥ 20px |
| 折弯冗余 | 每逻辑线 ≤ 1 拐点 | 主链必须 0 折弯 |
| HTML 完整性 | 无未闭合标签 | 用 HTMLParser 校验 |

## 常见坑（血泪教训）

1. **"为了对准顶点把直线改成斜线"是错的** → 应该挪块对齐，让线保持垂直。
2. **"块和菱形错位就加三段折线去绕"是错的** → 应该微调块中心 = 菱形顶点 x，线就直了。
3. **菱形是夹心饼干，别单独挪它** → 挪菱形上方的块。
4. **图例里的小菱形画不齐** → 干脆去掉图例。
5. **SVG 宽高写死导致缩放错位** → 用 `viewBox` 而不是固定 width/height 布局，或确保 viewBox 与内容匹配。

## 输出约定

- 生成的流程图嵌入**自包含单 HTML**，内联 `<svg>`，零外部依赖。
- 颜色约定：主链深蓝 `#192241`，AI `#eef2ff/#4338ca`，人工 `#f0fdf4/#10b981`，
  判断菱形 `#fff3cd/#d97706`，商务/辅助 `#f0f7ff/#2563eb`。
- 每张图渲染后主动跑校验脚本，全绿再交付。
